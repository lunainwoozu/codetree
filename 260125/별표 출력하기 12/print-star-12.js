const fs = require("fs");
const n = Number(fs.readFileSync(0));

for (let i = 0; i < n; i++) {
    for (let j = 0; j; j++) {
        if (i === 0) {
            process.stdout.write("* ");
        }
        if (j % 2 !== 0) {
            for (let k = n; k < ; k++) {}
        }
    }
    console.log();
}